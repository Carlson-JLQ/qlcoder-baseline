我先从这次生成的产物本身做一次质量审查，重点看三件事：建议是否和 probe 事实一致、是否真的可指导后续 agent 修复、以及当前输出里有没有误导性的地方。
        
            
toolName: view_files
            
status: success
          
            
filePath: /home/byteide/research/qlcoder/query-examples-java/CVE-2025-49656-iter1-repair-suggestions-test/repair_summary.md
          

        
            
toolName: view_files
            
status: success
          
            
filePath: /home/byteide/research/qlcoder/query-examples-java/CVE-2025-49656-iter1-repair-suggestions-test/selected_prompts.md
          

        
            
toolName: view_files
            
status: success
          
            
filePath: /home/byteide/research/qlcoder/query-examples-java/CVE-2025-49656-iter1-repair-suggestions-test/selected_repairs.json
          

        
            
toolName: view_files
            
status: success
          
            
filePath: /home/byteide/research/qlcoder/query-examples-java/CVE-2025-49656-iter1-probe-eval-default/probe_evaluation.json
          
**发现**
- 高: 当前输出对后续 agent 仍然不够“可执行”，因为缺少组件级的原始代码上下文和目标上下文绑定信息。[selected_prompts.md](file:///home/byteide/research/qlcoder/query-examples-java/CVE-2025-49656-iter1-repair-suggestions-test/selected_prompts.md) 里只有计数、对齐统计和通用 prompt，但没有把当前 query 中的 `isSource` / `isSink` / `isBarrier` / `isAdditionalFlowStep` 具体实现片段、目标方法名、目标文件名、以及最相关的命中样本直接塞进对应组件块里。结果是 agent 仍然要自己回头再解析 query 和 `probe_evaluation.json`，修复建议的“落地性”不足。
- 中: `repair_behavior` / `recommended_action` 和实际 prompt 文案还没有完全对齐，容易让 agent 选错动作。比如 [selected_repairs.json](file:///home/byteide/research/qlcoder/query-examples-java/CVE-2025-49656-iter1-repair-suggestions-test/selected_repairs.json) 已经把 `source` / `sink` 定成“`替换`”，但 [selected_prompts.md](file:///home/byteide/research/qlcoder/query-examples-java/CVE-2025-49656-iter1-repair-suggestions-test/selected_prompts.md) 里的文案仍然写着“add constraints ... or replace” / “refine the sink ...”，没有明确告诉 agent“优先直接替换原组件本体，而不是继续在旧实现上打补丁”。这会削弱你刚刚强调的设计原则。
- 中: 全局诊断信号没有被充分利用，导致组件建议虽然正确，但缺少“先后顺序”和“停止条件”。[repair_summary.md](file:///home/byteide/research/qlcoder/query-examples-java/CVE-2025-49656-iter1-repair-suggestions-test/repair_summary.md) 列出了四个组件建议，但没有把 `original_query` 的 `0/0` 失败事实和“先修 source/sink，再看 barrier/flow”的策略明确写出来。对于后续 agent，这种缺失会导致它把注意力分散到 `barrier` / `flow` 上，而不是先处理最可能的主因。
- 中: `barrier` 和 `flow` 的建议过于泛化，事实块不足以支撑具体修改。当前 [selected_prompts.md](file:///home/byteide/research/qlcoder/query-examples-java/CVE-2025-49656-iter1-repair-suggestions-test/selected_prompts.md) 只给了 `barrier` 的“19 vs 19”和 `flow` 的“2230 vs 2233”，但没有代表性 probe 行、没有 patch 邻域、没有“哪些 flow edge 最噪”或“哪些 barrier 分支最共享”的线索，所以 agent 只能收到“专化一下”“降噪一下”这类抽象建议，实际可操作性偏弱。
- 低: 输出里有部分信息噪音较高，但对决策帮助有限。比如 [selected_repairs.json](file:///home/byteide/research/qlcoder/query-examples-java/CVE-2025-49656-iter1-repair-suggestions-test/selected_repairs.json) 里的 `query_excerpt` 很长，却没有结构化定位到各组件相关片段；而 `source` 的 `120` 个命中文件、`181` 个命中方法只展示了计数，没有提炼出最接近目标修复区域的少量候选，阅读成本高但增益有限。

**改进建议**
- 把每个组件块补成“可直接修”的输入包：加入当前 predicate 源码片段、目标文件/目标方法、最相关的命中样本、以及一句明确的局部修改边界。
- 让 prompt 文案严格跟随 `repair_behavior`：`替换` 就明确写“replace the current source/sink body first”，`保留` 就不要再混入“可能替换”，避免动作漂移。
- 在总摘要里加入全局策略：明确写出“原 query 为 `0/0`，优先修 `source`/`sink`；只有在它们完成后仍失败，再处理 `barrier`/`flow`”。
- 给 `barrier` / `flow` 增加代表性证据摘要：不是只给总数，而是给 3 到 5 条最有代表性的 probe 结果或最接近 target 的候选事实。
- 压缩噪音字段：把大段 `query_excerpt` 改成按组件抽取的小片段，把海量 hit 统计改成“总量 + 最相关样本”。

**整体评价**
- 当前质量我会评成“**结构正确，方向基本对，但还不够面向执行**”。
- 好的地方是：状态判断和行为方向已经比之前的模板化产物稳很多，尤其 `source` / `sink` 给出了“替换”而不是继续叠加 `and`。
- 主要短板是：它现在更像“诊断报告”，还不像“后续 repair agent 的直接工作单”。

**最值得优先改的两点**
- 第一：把 `source` / `sink` 的 prompt 改成明确的“替换原组件本体”指令。
- 第二：给每个组件附上当前 query 的相关代码片段和最关键的 target facts。

如果你愿意，我下一步可以直接把这两点落到代码里，把 `selected_prompts.md` 和 `selected_repairs.json` 升级成真正适合 agent 消费的版本。